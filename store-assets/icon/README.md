# App Store icon

- `app-icon-1024.png` — 1024×1024 opaque RGB PNG (traffic-light 241 Runners Awareness branding)
- Source of truth in app: `../../assets/icon.png` (same asset; referenced by `app.config.ts`)

ASC uses the icon from the uploaded IPA. No separate marketing-logo upload for iOS.
