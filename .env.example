# 241RA Mobile - Environment Variables Template
# Copy this file to .env and add your actual values

# Backend API URL
EXPO_PUBLIC_API_URL=https://241runners-api-v2.azurewebsites.net

# Supabase (optional client-side storage / realtime)
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Mapbox (required for map screen)
# 1. Sign in at https://account.mapbox.com/
# 2. Tokens → Create token → Public (pk.*) with default scopes
# 3. Paste below, then rebuild the dev client (Mapbox is native-linked, not Expo Go)
# 4. Use the same token in website config.json MAPBOX_ACCESS_TOKEN
EXPO_PUBLIC_MAPBOX_ACCESS_TOKEN=

# Sentry (production error tracking)
EXPO_PUBLIC_SENTRY_DSN=
