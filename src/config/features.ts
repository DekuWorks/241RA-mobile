export const FEATURES = {
  /**
   * Admin and portal screens live in this repo for reuse in a dedicated admin app.
   * Disabled in the public mobile app until that app ships.
   */
  ADMIN_APP_ENABLED: false,
} as const;
